{
  "name": "standup-board",
  "scripts": {
    "start": "live-server frontend --port=3000",
    "dev": "concurrently \"npm run start\" \"python backend/app.py\""
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "concurrently": "^8.0.0"
  }
}
